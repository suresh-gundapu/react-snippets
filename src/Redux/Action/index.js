import { addTodo, deleteTodo } from "./todo_task";
import { deposite,withdraw,name, mobile, addTransactions } from "./account_task";

const actions = {addTodo,deleteTodo , deposite, withdraw, name, mobile, addTransactions};

export default actions;